schemaVersion: 1
execution:
  agentRunners:
    fake: { kind: fake, timeoutMs: 5000 }
  tiers: { standard: [fake] }
  defaultTier: standard
controlPlane: {}
integrations:
  primary:
    provider: fake
    enabled: true
    evidenceFile: provider/primary.json
  secondary:
    provider: fake
    enabled: true
    evidenceFile: provider/secondary.json
surfaces:
  api: { enabled: false }
  web: { enabled: false }
