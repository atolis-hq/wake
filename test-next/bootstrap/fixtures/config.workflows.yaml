workflows:
  default:
    stages:
      implement:
        activity: implement
        with:
          prompt: implement
        execution:
          workspace: none
          tier: standard
        on:
          done:
            then: done
