workflows:
  default:
    stages:
      implement:
        activity: implement
        with:
          prompt: implement
        execution:
          workspace: none
          runnerPool: standard
        on:
          done:
            then: done
