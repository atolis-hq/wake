# Wake Functional Decision Catalogue

The catalogue records disposition of legacy capability evidence. It does not
define target architecture. Authority order is the target design, `MODULE.md`,
public contracts, target scenarios, target code, this catalogue, then legacy
evidence.

Legacy code, tests, configuration, documentation, and scripts are evidence of
intent and learned constraints only. No catalogue row can override the target
model or approve implementation.

**Disposition review:** Approved 2026-07-30 by operator

**Disposition review:** Approved 2026-08-01 by operator for Task 25A —
`WORK-IDENTITY-MINTED`, `ORCH-SELECTOR`, `ORCH-APPROVAL-AUTHORITY`,
`ORCH-CONFIG-DISCOVERY`, `ACT-AGENT-PROMPT`, `FUTURE-STATE-SYNC-EXTRACTION`.

Rows deliberately consolidate several legacy paths under one target mechanic.
Target scenario identifiers are prospective stable specifications for the
rewrite. Rows removed from or deferred beyond the initial rewrite have no
target scenario because they are outside its executable scope.

| ID | Intent or learned constraint | Underlying mechanic | Disposition | Target owner | Target scenarios | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| WORK-INTAKE | Accept eligible external or scheduled objectives once, mint a Wake identity before linking the origin, reject identity-less input, and survive duplicate delivery or a crash between creation and linking. Legacy source policy remains useful evidence, but no provider may assign a WorkItem identity. | A canonical intake command is validated by Work, mints a WorkItem, and requests typed Resource correlation using stable command and source-event identities. | correct | `work` with `resources` and `integrations` | E2E-WORK-001, E2E-WORK-002 | `test/adapters/fake-ticketing-system.test.ts`<br>`test/core/event-resolver.test.ts`<br>`test/core/mint-qualification.test.ts`<br>`test/core/tick-runner.intake.test.ts` |
| WORK-LIFECYCLE | Preserve intrinsic work facts such as creation, closure, freeze, deletion, and completion while correcting the legacy conflation of work status with workflow stage or Run sentinel. A WorkItem must survive closure or loss of one external representation. | Work commands produce work-owned lifecycle events; workflow position and attempt state are joined through typed references, not stored on WorkItem. | correct | `work` | E2E-WORK-003, E2E-LIFECYCLE-001, E2E-LIFECYCLE-002, E2E-LIFECYCLE-003, E2E-LIFECYCLE-005 | `test/core/lifecycle-service.test.ts`<br>`test/domain/work-item-labels.test.ts`<br>`test/domain/work-item-status.test.ts`<br>`test-next/e2e/scenarios/lifecycle-happy-path.test.ts`<br>`test-next/e2e/scenarios/lifecycle-retry-recovery.test.ts`<br>`test-next/e2e/scenarios/lifecycle-retry-limit.test.ts`<br>`test-next/e2e/scenarios/lifecycle-unattended-opt-out.test.ts` |
| WORK-COMMAND | Operators need idempotent commands to create, freeze, unfreeze, delete, retry, and request a named agent capability. Command-like text must be recognized only in the expected signal context and must not silently advance unrelated workflow state. | Typed Work commands validate current lifecycle and actor authority; requested Activities are separate Orchestration commands with their own idempotency keys. | preserve | `work` and `orchestration` | E2E-ORCH-COMMAND-001 | `test/core/approval-intents.test.ts`<br>`prompts/ask.md`<br>`prompts/codereview.md` |
| ORCH-DARK-FACTORY | Preserve the bounded review-watch workflow: review children gate parent progress, child retries do not self-trigger, and a human decision remains authoritative. | Ordinary watch-gated child workflows use causal identities and typed verdict signals; operator signals can resolve a gate without a child. | consolidate | `orchestration` with `activities` | E2E-DARKFACTORY-001, E2E-DARKFACTORY-002, E2E-DARKFACTORY-003, E2E-DARKFACTORY-004, E2E-DARKFACTORY-005 | `test-next/e2e/scenarios/dark-factory-happy-path.test.ts`<br>`test-next/e2e/scenarios/dark-factory-loop-guard.test.ts`<br>`test-next/e2e/scenarios/dark-factory-child-error.test.ts`<br>`test-next/e2e/scenarios/dark-factory-watch-trigger-scoping.test.ts`<br>`test-next/e2e/scenarios/dark-factory-human-override.test.ts` |
| RESOURCE-IDENTITY | Keep canonical Resource identity, exact locator preservation, capabilities, kind, and provenance, while correcting provider-shaped canonical records and guessed or parsed locators in core logic. Artifact identity is untrusted until the owning domain verifies it. | Resources canonicalizes adapter evidence into specialist Resource records and exposes typed identity and capability queries without making GitHub the core model. | correct | `resources` | E2E-WORK-001, E2E-PR-001 | `test/adapters/github-artifact-verifier.test.ts`<br>`test/domain/resource-uri.test.ts`<br>`test/lib/work-id.test.ts` |
| RESOURCE-CORRELATION | Preserve automatic and operator-declared correlation, retraction, provenance, roles, reverse lookup, duplicate suppression, and replayability. Correlation is a typed relation between Wake-owned entities, not borrowed work identity. | Resource correlation commands emit domain-owned relation facts; pure projections rebuild a reverse index from the journal. | preserve | `resources` using `kernel` relation primitives | E2E-WORK-001 | `test/adapters/resource-index.test.ts`<br>`test/cli/correlate-command.test.ts`<br>`test/core/event-builders.test.ts`<br>`test/core/projection-updater.test.ts` |
| RESOURCE-CONFLICT | A missing, malformed, ambiguous, conflicting, stale, or untrusted Resource must block a destructive or identity-sensitive action. Correct the legacy downgrade and last-write behavior where it can hide competing primary ownership. | Specialist cardinality and trust policy resolves one expected Resource or returns an explicit conflict result with provenance; no unsafe caller chooses a winner. | correct | `resources` and the consuming specialist Activity | E2E-PR-001, E2E-SECURITY-001 | `test/core/tick-runner.approval.test.ts`<br>`test/core/tick-runner.invariants.test.ts`<br>`docs/events.md` |
| ORCH-CONFIG | Keep configured workflows, entry stages, Activity input, execution hints, outcome routing, waits, bounded repeats, and selectors, but consolidate validation under owning modules. Correct the global schema and legacy fields that place workspace, runner, and stage on the wrong owner. | Bootstrap composes module schemas; Orchestration compiles its `on` transitions, Activities validate `with`, and Execution validates its execution block. | consolidate | `orchestration`, `activities`, `execution`, and `bootstrap` | E2E-CONFIG-001 | `test/config/discover-config-files.test.ts`<br>`test/config/load-config.test.ts`<br>`test/config/split-config.test.ts`<br>`test/domain/config-schema-split.test.ts`<br>`test/domain/schema.test.ts`<br>`test/lib/deep-merge.test.ts`<br>`test/lib/yaml-file.test.ts`<br>`docs/configuration.md`<br>`docs/workflows.md`<br>`src/domain/schema.ts` |
| ORCH-TRANSITION | Preserve deterministic entry, outcome routing, completion, and invalid-route blocking while correcting the legacy model that treats a stage as WorkItem state and mixes policy, labels, Run results, and transitions. | A pure interpreter evaluates a validated WorkflowDefinition and one primary WorkflowInstance position, then an application service appends accepted transition and activation facts. | correct | `orchestration` | E2E-GOLDEN-001, E2E-ORCH-RETRY-001 | `test/domain/workflows.test.ts`<br>`test/core/lifecycle-service.test.ts`<br>`test/core/policy-engine.test.ts` |
| ORCH-WAIT | Preserve waiting for human or external developments, including expected approval, requested changes, questions, duplicate replies, and resumption. Correct comment heuristics into typed, validated signals tied to the waiting WorkflowInstance and expected revision. | WorkflowInstance records a typed wait contract; only a current accepted signal with matching identity and authority may resume it once. | correct | `orchestration` with the signal-owning Activity | E2E-ORCH-WAIT-001, E2E-SIGNAL-001 | `test/core/tick-runner.approval.test.ts`<br>`test/core/policy-engine.test.ts` |
| ORCH-CHILD | Preserve subordinate review or follow-on workflows while correcting special nested loops and parent projection mutation. A child belongs to the same orchestration group, does not create a second primary WorkItem position, and its completion is consumed once. | Linked WorkflowInstances and causally linked ActivityActivations carry parent activation, group, trigger, and cycle identities with once-only completion consumption. | correct | `orchestration` | E2E-ORCH-CHILD-001 | `test/core/tick-runner.test.ts` |
| ORCH-WATCH | Preserve useful event- or schedule-triggered follow-on work, retry caps, and parent gating, but consolidate legacy watchers into ordinary durable activations instead of a second workflow engine. Successful children must not reset group budgets or trigger themselves. | Checkpointed reactors propose idempotent ActivityActivations with causal-cycle identity; Orchestration enforces group-wide repeat and dispatch budgets. | consolidate | `orchestration` with `control-plane` dispatch policy | E2E-ORCH-LOOP-001 | `test/core/tick-runner.test.ts`<br>`test/core/stale-run-reconciler.test.ts` |
| EXEC-RUN | Preserve durable claim-before-launch, leases, timestamps, executor identity, supervision, and exactly one terminal result while correcting the broad legacy run record. Each effectful attempt for one ActivityActivation is a distinct Run. | Execution owns a narrow Run aggregate and repository; stable Activation identity links policy intent to one or more explicitly authorized attempts. | correct | `execution` | E2E-GOLDEN-001, E2E-EXEC-001, E2E-ORCH-RETRY-001 | `test/adapters/claude-runner.test.ts`<br>`test/adapters/codex-runner.test.ts`<br>`test/adapters/cursor-runner.test.ts`<br>`test/core/run-lease.test.ts`<br>`test/core/tick-runner.run-recording.test.ts` |
| EXEC-RESULT | Preserve transport parsing, structured output, usage, process evidence, cancellation, and failure classification while correcting the collapse of process state, agent sentinel, workflow outcome, and provider effects. Raw agent output is evidence only. | A typed Run result envelope separates execution outcome from Activity-owned validated outcome; Orchestration sees only the accepted Activity outcome. | correct | `execution` and the invoked `activities` capability | E2E-EXEC-001 | `test/domain/schema.test.ts`<br>`test/domain/work-item-status.test.ts`<br>`prompts/implement.md` |
| EXEC-ROUTING | Preserve named Claude, Codex, Cursor, script, deterministic, and fake executors, runner pins, tier fallbacks, timeouts, and capability differences, but consolidate duplicated runner command and result handling behind Execution ports. | Execution selects a registered executor from Activity requirements and resolved execution policy, records the decision, and invokes one adapter through a narrow contract. | consolidate | `execution` | E2E-EXEC-001 | `test/adapters/runner-registry.test.ts`<br>`test/core/tick-runner.quota.test.ts` |
| EXEC-WORKSPACE | Preserve no-workspace, read-only, and isolated branch workspace capabilities, reuse checks, merge-conflict detection, bookkeeping, cleanup, and visible scratch paths. No Activity receives a workspace unless it declares the capability. | Workspace selection and lifecycle are optional Execution capabilities referenced by Run; cleanup facts do not alter the accepted Activity outcome. | preserve | `execution` | E2E-EXEC-001 | `test/adapters/git-workspace-manager.test.ts`<br>`test/core/tick-runner.workspace-cleanup.test.ts`<br>`test/core/workspace-cleanup.test.ts` |
| EXEC-CANCEL | Preserve operator interruption, closure-triggered cancellation, timeout, supersession, and graceful shutdown, while correcting cancellation that is inferred from process state. A cancellation request and its observed outcome are separate durable facts. | Execution signals a Run through lease and process ports, reconciles what happened, and records cancelled, completed, failed, or ambiguous outcome. | correct | `execution` | E2E-EXEC-CANCEL-001 | `test/core/tick-runner.dispatch.test.ts`<br>`test/cli/stop-command.test.ts` |
| EXEC-RECOVERY | Preserve startup reconciliation before dispatch, stale lock detection, missing-record recovery, supersession, and recoverable results. Correct unsafe conversion of unknown external or process state into assumed failure and retry. | Execution reconciles active Runs as not-started, running, completed, failed, or externally ambiguous; Orchestration separately decides any new attempt. | correct | `execution` | E2E-EXEC-RECOVER-001 | `test/core/stale-run-reconciler.test.ts`<br>`test/core/tick-runner.reconcile.test.ts`<br>`docs/execution-invariants.md` |
| ACT-AGENT | Preserve configured judgment-rich agent prompts and structured outcomes, including refine, implement, revise, ask, and triage intent. Correct the authority boundary so agents receive minimal typed context and cannot choose stage, provider mutation, merge, or retry policy. | A named agent Activity declares typed input, Resource requirements, outcomes, and Execution capabilities; validated output becomes Activity outcome evidence. | correct | `activities/agent` | E2E-EXEC-001, E2E-SECURITY-001 | `test/adapters/prompt-templates.test.ts`<br>`prompts/refine.md`<br>`prompts/revise.md`<br>`prompts/triage-assign.md` |
| ACT-REVIEW | Preserve plan and code review verdicts, artifact reporting, changes-requested feedback, and uncertain-result suppression. Correct guessed PR identity, unverified artifacts, and verdicts not bound to the reviewed subject. | Review Activities require an exact trusted subject Resource, emit typed verdicts and evidence, and cannot themselves approve, merge, label, or push. | correct | `activities/review` | E2E-ORCH-COMMAND-001, E2E-PR-001 | `test/adapters/github-artifact-verifier.test.ts`<br>`test/adapters/prompt-templates.test.ts`<br>`prompts/plan-review.md`<br>`prompts/pr-review.md` |
| ACT-PR-APPROVE | Preserve explicit PR approval as an independently auditable specialist action, but correct issue-comment approval, agent DONE, or stale bot markers being treated as general approval authority. Approval is bound to the exact verified revision assessed. | `pr.approve` resolves one trusted pull-request Resource, checks actor and policy authority, records revision-bound evidence, and emits provider-neutral approval intent. | correct | `activities/pr/approve` | E2E-PR-APPROVE-001, E2E-PR-002 | `test/core/tick-runner.approval.test.ts`<br>`test/cli/build-runtime-pr-gating.test.ts` |
| ACT-PR-MERGE | Preserve configured merge method and safety gates as a distinct operator capability. Correct direct or auto-merge triggered by an agent sentinel, unverified ownership, stale approval, or assumed provider failure. | `pr.merge` requires exactly one trusted pull-request Resource and current revision-bound approval, then emits a provider-neutral merge intent whose delivery can be confirmed, failed, or ambiguous. | correct | `activities/pr/merge` | E2E-PR-MERGE-001, E2E-PR-MERGE-002, E2E-PR-MERGE-003 | `test/adapters/github-pull-request-merge-actor.test.ts`<br>`test/core/tick-runner.approval.test.ts`<br>`docs/workflows.md` |
| CONTROL-TICK | Preserve bounded one-shot progress and deterministic fake-driven testing, but consolidate intake, orchestration, execution, and delivery coordination behind one `advanceOnce(scope, budget)` application capability. A tick is a host, not a workflow engine. | The CLI host invokes shared Control Plane selection and advancement until its explicit budget produces no-work, progressed, waiting, blocked, or exhausted. | consolidate | `control-plane` with `surfaces/cli` host | E2E-CONTROL-002 | `test/core/tick-runner.dispatch.test.ts`<br>`test/core/tick-runner.run-recording.test.ts`<br>`test/scripts/e2e-github-fake.test.ts` |
| CONTROL-RESIDENT | Preserve repeated progress, durable wake requests, idle backoff, pause gates, status changes, and graceful stop while consolidating the split and legacy resident loops onto the same `advanceOnce` behavior as tick and schedule hosts. | A resident host owns sleep, wake signals, backoff, and process lifetime only; shared Control Plane policy owns eligibility and budgets. | consolidate | `control-plane` and `bootstrap` host composition | E2E-CONTROL-002 | `test/cli/control-plane.test.ts`<br>`src/main.ts` |
| CONTROL-SCHEDULE | Preserve cron-style scheduled work and WIP gating, but consolidate schedules into durable slot commands or signals rather than a special synthetic workflow execution path. Slot identity must be idempotent across restarts. | A scheduler host emits one durable signal per elapsed slot; ordinary intake and `advanceOnce` process it under shared policy. | consolidate | `control-plane` | E2E-SCHEDULE-001 | `test/core/scheduled-workflow-source.test.ts` |
| CONTROL-FAIRNESS | Preserve deterministic eligibility recheck, single-flight safety, global dispatch caps, and progress for intake while execution is active. Correct lexicographic WorkItem order and per-child counters where they can starve work or reset a causal budget. | Control Plane applies explicit fairness, activation leases, group-wide budgets, and a final per-advance cap identically across all hosts. | correct | `control-plane` | E2E-CONTROL-001 | `test/core/tick-runner.invariants.test.ts`<br>`test/core/tick-runner.dispatch.test.ts`<br>`test/lib/lock.test.ts` |
| CONTROL-QUOTA | Preserve classified runner quota pauses, reported reset times, bounded fallback backoff, alternate eligible runner selection, and operator unpause. Quota is dispatch availability, not a workflow result. | Control Plane eligibility consumes Execution runner-health views and budget policy without embedding provider quota semantics in Work or Orchestration. | preserve | `control-plane` with `execution` health views | E2E-CONTROL-003 | `test/core/quota-backoff.test.ts`<br>`test/core/tick-runner.quota.test.ts` |
| INT-GITHUB-ISSUE | Preserve GitHub issue discovery, policy filtering, overlap polling, per-repository fault isolation, comment and label translation, echo detection, and source checkpoints. Correct issue-shaped canonical WorkItems and provider labels as workflow truth. | A GitHub adapter owns payloads and watermarks, translates evidence into canonical commands, and formats provider effects without leaking GitHub types into core modules. | correct | `integrations/github` | E2E-WORK-002, E2E-WORK-003 | `test/adapters/github-auth.test.ts`<br>`test/adapters/github-client.test.ts`<br>`test/adapters/github-client-request-log.test.ts`<br>`test/adapters/github-etag-cache.test.ts`<br>`test/adapters/github-issues-work-source.test.ts` |
| INT-GITHUB-PR | Preserve PR discovery, reviews, review threads, required-check evidence, closed-PR handling, pagination fallbacks, bot attribution, and stable thread identity. Correct direct watcher and core consumption of provider payloads. | The GitHub PR adapter emits namespaced evidence and proposes canonical Resource, Review, or signal commands; owning domains validate before accepting facts. | correct | `integrations/github` | E2E-PR-001 | `test/adapters/github-pull-request-activity-source.test.ts`<br>`test/cli/build-runtime-pr-gating.test.ts` |
| INT-HUMAN-SIGNAL | Preserve explicit approval, changes-requested, interrupt, question, and reply-to-continue intent while correcting slash-command substring matching, stale or duplicate comments, bot confusion, and provider comments treated as canonical decisions. | Integration verifies and parses provider evidence; the owning Review or Approval capability validates actor, Resource, revision, expected state, and idempotency before emitting a typed signal. | correct | `integrations` plus the signal-owning `activities` capability | E2E-SIGNAL-001, E2E-SECURITY-001 | `test/core/approval-intents.test.ts`<br>`test/core/policy-engine.test.ts`<br>`test/core/tick-runner.approval.test.ts` |
| INT-PUBLISH | Preserve replies to origin, explicit subscriptions, PR-thread replies, attribution, status publication, label synchronization, and no-drop fallback behavior, but consolidate routing and formatting as directional Integration behavior. Projections must never publish. | Canonical outbound intent is projected for delivery; adapter routing uses target Resource and provenance, with stable idempotency and echo markers. | consolidate | `integrations` | E2E-DELIVERY-001 | `test/core/sink-router.test.ts`<br>`test/adapters/github-issues-work-source.test.ts`<br>`test/adapters/github-pull-request-activity-source.test.ts`<br>`test/domain/work-item-labels.test.ts` |
| INT-OUTBOX | Preserve durable unresolved outbound intents, bounded failure handling, confirmation, and reconciliation. Correct dead-letter or retry behavior that treats a sent-but-unconfirmed external effect as ordinary failure or overwrites the originating Run. | The journal records intent and delivery-result facts; an opt-in outbox projection claims by stable key and records confirmed, failed, or ambiguous outcomes before policy chooses reconciliation. | correct | `integrations` using `kernel` journal and checkpoint ports | E2E-DELIVERY-001, E2E-FAULT-001, E2E-DUPLICATE-001 | `test/core/outbox.test.ts`<br>`test/core/tick-runner.outbox.test.ts` |
| SURFACE-CLI | Preserve a discoverable `wake` command surface, root selection, help and version behavior, sandbox delegation, audit, correlate, tick, start, stop, smoke, UI, init, and doctor. Commands must call public applications and views, not stores or adapters directly. | CLI parsing and presentation live in Surfaces; Bootstrap composes dependencies and host processes. | preserve | `surfaces/cli` and `bootstrap` | E2E-SURFACE-001 | `test/bin/wake-dev.test.ts`<br>`test/cli/audit-command.test.ts`<br>`test/cli/main.test.ts`<br>`bin/package.json`<br>`bin/wake-dev.js`<br>`docs/cli.md`<br>`package.json`<br>`README.md` |
| SURFACE-API | Preserve loopback-scoped operator reads and bounded mutations for status, WorkItems, events, metrics, transcripts, pause, retry, freeze, and delete. Correct direct filesystem, adapter, and flattened projection access. | The unauthenticated `/api/v1` HTTP Surface binds to loopback by default, invokes public application commands, composes nested domain views, and returns explicit validation and conflict results. Remote authenticated exposure remains deferred. | correct | `surfaces/api` | E2E-SURFACE-001, E2E-SURFACE-BOARD-001, E2E-SURFACE-ANALYTICS-001 | `test-next/surfaces/api-routes.test.ts`<br>`test-next/surfaces/web-server.test.ts`<br>`test/adapters/ui-server.test.ts` |
| SURFACE-UI | Preserve operator board, run status, metrics, event feed, transcript and manual-control intent, but correct issue-shaped presentation as the canonical read model and effectful or scan-heavy projection behavior. | The React browser Surface in `surfaces/web` renders replaceable domain views and submits typed API commands; cross-domain summaries keep `work`, `orchestration`, `execution`, `resources`, and `activities` nested. | correct | `surfaces/web` | E2E-SURFACE-001, E2E-SURFACE-BOARD-001, E2E-SURFACE-ANALYTICS-001 | `src-next/surfaces/web/e2e/operator-journey.spec.ts`<br>`test/adapters/ui-data.test.ts`<br>`test/adapters/ui-server.test.ts`<br>`test/lib/format.test.ts` |
| OPS-INIT | Preserve scaffolding of a human-readable Wake root, prompts, workspace directory, source or packaged development mode, and refusal to overwrite a non-empty target. Runtime state remains under `.wake`. | Bootstrap-owned initialization writes versioned configuration and shipped assets without creating compatibility or migration state. | preserve | `bootstrap` with `surfaces/cli` | E2E-OPS-INIT-001 | `test/cli/init-command.test.ts`<br>`test/cli/scaffold-assets.test.ts`<br>`templates/SETUP.md` |
| OPS-DOCTOR | Preserve host-side diagnostics for configuration, prompts, authentication, Docker, sandbox reachability, versions, customized shipped assets, and target state health. Ordinary diagnostics distinguish failures from notices and never mutate canonical events; explicit `--rebuild-projections` may clear and replay derived views. | A Bootstrap diagnostic application queries module validation and adapter probes; CLI formats results and, after explicit operator selection, calls an injected `ProjectionMaintenance` surface port to rebuild projections without importing Persistence. | preserve | `bootstrap` and `surfaces/cli` | E2E-OPS-001 | `test/cli/doctor-command.test.ts`<br>`test/cli/startup-preflight.test.ts` |
| OPS-SANDBOX | Preserve source and packaged Docker builds, stable generated Dockerfile ownership, mounts, auth setup, up, update, stop, exec, logs, resume, UI and resident wiring, and safe command forwarding. Sandbox remains a deployment host, not a domain. | Bootstrap selects sandbox process adapters; CLI owns subcommands and Docker infrastructure owns container mechanics with bounded logs and graceful shutdown. | preserve | `bootstrap` and `surfaces/cli` | E2E-OPS-SANDBOX-001 | `test/adapters/docker-cli.test.ts`<br>`test/cli/sandbox-command.test.ts`<br>`test/cli/sandbox-entrypoint-command.test.ts`<br>`test/cli/sandbox-resume.test.ts`<br>`test/cli/sandbox-setup-command.test.ts`<br>`docker/Dockerfile`<br>`docker/Dockerfile.packaged`<br>`docker/entrypoint.sh`<br>`docker/setup.sh` |
| OPS-SELF-UPDATE | Preserve source-mode tag discovery, bounded ledger, interrupted-update recovery, issue reporting, container rebuild, and packaged-mode refusal. Before changing this installation, safely stop the whole Wake runtime, drain existing Runs, durably cancel only after bounded grace, and resume only after healthy update/recovery. Failed tags must not loop endlessly, but a newly published tag must still be tried once. Updating must not become workflow or persistence migration architecture. | Bootstrap persists a maintenance lease beside the ledger. Its existing composed pause gate stops intake, projections, runner/tick work, recovery, reconciliation, and delivery; the update service drains public Run views, uses Execution's maintenance cancellation reason, and owns Git/Docker checkout, health, rollback, bad-tag selection, and PID-liveness-protected attempt exclusion. CLI exposes bounded and resident forms. | preserve | `bootstrap`, `control-plane`, `execution`, and `surfaces/cli` | E2E-OPS-SELF-UPDATE-001, E2E-OPS-SELF-UPDATE-002, E2E-OPS-SELF-UPDATE-003, E2E-OPS-SELF-UPDATE-004, E2E-OPS-SELF-UPDATE-005, E2E-OPS-SELF-UPDATE-006, E2E-OPS-SELF-UPDATE-007, E2E-OPS-SELF-UPDATE-008 | `test/adapters/self-update-ledger.test.ts`<br>`test/cli/self-update-command.test.ts`<br>`test/version.test.ts`<br>`test-next/integration/bootstrap/update-maintenance-lease.test.ts`<br>`test-next/unit/bootstrap/self-update-application.test.ts`<br>`test-next/e2e/scenarios/self-update-maintenance.test.ts` |
| OPS-TRANSCRIPT | Preserve opt-in runner transcripts, real session identity, prompt-first ordering, retention after workspace cleanup, disabled behavior, and UI retrieval. Transcripts are operational artifacts, not journal truth or Activity outcomes. | Execution-owned transcript adapters store artifacts under `.wake/transcripts`; retention and access are explicit configuration and views. | preserve | `execution` with `surfaces` views | E2E-OPS-TRANSCRIPT-001 | `test/adapters/runner-transcripts.test.ts`<br>`test/core/tick-runner.workspace-cleanup.test.ts` |
| WORK-IDENTITY-MINTED | A WorkItem and a Resource identity are minted by Wake and never derived from an external key, a provider name, or a schedule slot. External references are carried only as `ExternalResourceKey` on the Resource, and reverse lookup is a journal-folded projection rather than a reconstructable id or a hand-maintained mapping store. | Identity brands accept only a minted ULID shape; intake looks up the external key, correlates to the existing WorkItem on a hit, and mints both plus a correlation on a miss. | correct | `work` and `resources` with `integrations` and `control-plane` | E2E-WORK-002, E2E-SCHEDULE-001, E2E-LIVE-009 | `src-next/integrations/github/application/inbound-translator.ts`<br>`src-next/control-plane/application/schedule-service.ts`<br>`docs/adrs/0001-correlating-external-resources-to-work-items.md` |
| ORCH-SELECTOR | Adapter configuration assigns operator-authored tags at intake; Orchestration selectors match those tags, the adapter id, and resource kind to choose a workflow, falling through to a configured default. Adapters never propose a workflow name, and an intake rule may not tag from a Wake-owned marker family. | Tags are first-class WorkItem data carried on the intake command; a pure selector evaluates configured `match` clauses in order and Orchestration starts the chosen workflow for a new WorkItem. | correct | `orchestration` with `work` and `integrations` | E2E-LIVE-001, E2E-LIVE-005 | `src/domain/workflows.ts`<br>`docs/workflows.md` |
| ORCH-APPROVAL-AUTHORITY | What may satisfy a wait is first-class and closed: a human, Wake resolving deterministically, or a named watch's verdict. Auto-resolution requires both a workflow-declared capability and durable per-item operator consent, so an agent prompt can no longer widen approval authority. Provenance is recorded separately from authority. | A workflow route declares `await.from` as a discriminated authority union; Work owns an idempotent consent flag; the provider translates its own affordance to a neutral signal. | correct | `orchestration` with `work` and `integrations` | E2E-ORCH-WAIT-001, E2E-LIVE-003 | `src/core/policy-engine.ts`<br>`prompts/implement.md`<br>`docs/workflows.md` |
| ORCH-CONFIG-DISCOVERY | Remove because alphabetical multi-file `config*.yaml` merge and the `config.json` fallback make the effective configuration unpredictable to read and to support, and no operator capability depends on them. The target reads exactly `config.yaml` then `config.workflows.yaml`, deep-merged in that order. | Bootstrap loads two fixed files from the Wake root and validates each module subtree through its owning schema. | remove | `bootstrap` | — | `src/config/discover-config-files.ts`<br>`docs/configuration.md` |
| ACT-AGENT-PROMPT | Prompt frontmatter is parsed as YAML and validated against a typed schema, so an operator edit either takes effect or reports a clear error naming the file and the issue. `maxTurns` is optional and passed to the runner verbatim when present; Wake injects no default and clamps no value. `allowAutoApproval` is not a frontmatter field. | Templates in `<wakeRoot>/prompts` are parsed with the `yaml` package into a validated typed record; `wake doctor` validates every template before a run. | correct | `activities/agent` with `execution` | E2E-PROMPT-001 | `src/adapters/prompt-templates.ts`<br>`src/core/stage-prompt.ts`<br>`test/adapters/prompt-templates.test.ts` |
| FUTURE-STATE-SYNC-EXTRACTION | Defer because the marker-family reconciliation policy is provider-agnostic set arithmetic and will eventually be shared, but building a shared capability against a single real provider is speculative generality. It is sited inside the GitHub namespace as a pure function with no provider types in its signature, so extraction is a file move plus a registration. The trigger is the second provider that needs it. | A pure family-preserving reconciler replaces only Wake-owned markers and preserves every other user marker; vocabulary and transport stay provider-owned. | defer | `integrations/github` until a second provider needs it | — | `src/adapters/github-issues-work-source.ts` |
| PERSIST-JOURNAL | Preserve a local append-only event record, deterministic ordering, audit after projection deletion, atomic expected-sequence append, and replay after crash, while consolidating the legacy StateStore facade into one narrow journal port and domain repositories. | One physical JSONL journal under `.wake/events` provides logical streams and global positions; canonical events are facts and adapter evidence stays namespaced. | consolidate | `kernel` port with `persistence` filesystem adapter | E2E-JOURNAL-001 | `test/adapters/state-store.test.ts`<br>`test/core/tick-runner.invariants.test.ts`<br>`test/domain/event-types.test.ts`<br>`test/lib/paths.test.ts`<br>`docs/events.md` |
| PERSIST-PROJECTION | Preserve rebuildable WorkItem, Resource, Run, delivery, and operator views, including crash-idempotent last-position tracking. Correct projection code that appends events, delivers effects, updates unrelated stores, reads clocks, or acts as truth. | Pure domain projectors fold events into replaceable views; filesystem projection and checkpoint adapters atomically persist view plus global position. | correct | Owning domain projectors with `kernel` ports and `persistence` adapter | E2E-PROJECTION-001 | `test/core/projection-updater.test.ts`<br>`test/adapters/state-store.test.ts` |
| OPS-PROCESS | Preserve atomic file locks, stale PID and start-identity detection, bounded log rotation, detached process logging, and captured sandbox command logs. Consolidate them as low-level infrastructure used only by appropriate hosts and adapters. | Focused filesystem and process primitives implement locking and logging without owning domain policy or durable business facts. | consolidate | `persistence`, `execution`, or `bootstrap` according to use | E2E-OPS-SANDBOX-001 | `test/lib/detached-process-logging.test.ts`<br>`test/lib/lock.test.ts`<br>`test/lib/log-rotation.test.ts`<br>`test/cli/sandbox-exec-logging.test.ts`<br>`docker/log-command.sh` |
| FUTURE-SNAPSHOT | Defer because snapshots are a valid measured replay optimization, but the initial filesystem journal must first prove that replay performance is a problem. Snapshot machinery must not become a second source of truth. | A future repository-internal optimization may load a verified snapshot and replay later journal positions. | defer | `persistence` behind domain repositories | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-EVENT-UPCASTING | Defer because incompatible target event evolution is not needed during the pre-release rewrite; adding an upcaster framework now would create speculative compatibility architecture. | Future schema evolution may add domain-owned readers or migrations only when an accepted released schema requires them. | defer | Event-owning modules with `persistence` readers | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-WORKFLOW-VERSIONING | Defer because historical versioned WorkflowDefinition entities are valid for long-running definition evolution but excluded initially; a definition fingerprint is audit metadata only. | Future Orchestration may resolve an instance against a persisted immutable definition version. | defer | `orchestration` | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-GRAPH-MUTATION | Remove because unrestricted arbitrary nodes, edges, and `addEdge` mutation weaken domain ownership and are not a target capability. The typed relation catalogue already provides the required traversal seam. | Domains own valid relation commands and events over Kernel relation primitives. | remove | `kernel` primitives and relation-owning domains | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-WORK-DEPENDENCY-POLICY | Defer because typed WorkItem links are valid now, but general dependency and priority semantics require product policy not approved for the initial rewrite. | Future Work or Control Plane policy may interpret registered WorkItem relations for eligibility. | defer | `work` and `control-plane` | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-PARALLEL-POSITIONS | Defer because multiple concurrent primary workflow positions are a valid extension, but the initial interpreter intentionally maintains one primary position per WorkflowInstance. | Stable ActivityActivation and causation identities leave a future concurrency seam without parallel state today. | defer | `orchestration` | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-WORKFLOW-TRANSFER | Defer because transferring active work between workflows may later supersede and relink WorkflowInstances, but exposing it now would require unapproved repair and history policy. | A future command supersedes one instance, creates another at a selected entry, and relinks the WorkItem active-workflow reference. | defer | `orchestration` and `work` | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-AGENT-GRAPHS | Remove because free-form agent-authored workflow graphs bypass configured authority, validation, bounded cycles, and deterministic transition policy. Agents may return typed outcomes, not executable orchestration structure. | Validated operator-authored WorkflowDefinitions remain the only graph-like orchestration input. | remove | `orchestration` | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-PLUGIN-PLATFORM | Remove because a universal plugin or capability platform is unwanted speculative infrastructure for the rewrite; concrete public ports and specialist Activities are sufficient. | Extensions implement explicit module-owned contracts and are selected only by Bootstrap. | remove | Contract-owning modules and `bootstrap` | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-COMMUNICATION-DOCUMENT-ACTIVITIES | Defer because specialist conversation and document Activities are valid long-term product capabilities but are outside the initial SDLC Activity scope. Existing provider messages remain Integration responsibilities. | Future specialist Activities may declare typed inputs, Resources, outcomes, and effects through the same Activity contract. | defer | Future `activities` specialists | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| FUTURE-DATABASE-ADAPTER | Defer because a database persistence adapter is a credible replacement behind Kernel ports, but the filesystem adapter is the only required rewrite implementation. Building both would dilute the initial proof. | Bootstrap may later select another implementation of EventJournal, projection, and checkpoint ports. | defer | `persistence` and `bootstrap` | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
| ARCH-MIGRATION-COMPATIBILITY | Remove because migration, backward-compatible schemas, dual running, dual writes, and production-transition layers would preserve obsolete legacy models and create architecture used only during the rewrite. The approved replacement is alongside legacy with a final mechanical switch. | Target modules build new state from target events; legacy remains read-only evidence until final removal. | remove | Rewrite method, enforced by architecture checks | — | `docs/superpowers/specs/2026-07-30-wake-target-architecture-design.md` |
