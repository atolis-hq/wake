currently wake picks up any ticket it finds.

we should have a configurable policy which allows us to define the state of a ticket at the point it is picked up. (possibly but not necessarily applied when it is continued)

examples of optional matchers

- assigned to a user
- has a label eg : wake

