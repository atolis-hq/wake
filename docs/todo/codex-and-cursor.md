add support for codex cli and cursor cli.

given we then support multiple agents, the setup process may need to change to allow optionality.

we should be able to configure which cli and model to use for each step. 

consider how we could also override the model/cli used for a particular step