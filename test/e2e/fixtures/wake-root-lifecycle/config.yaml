schemaVersion: 1
execution:
  agentRunners:
    fake: { kind: fake }
  runnerPools: { standard: [fake] }
  defaultRunnerPool: standard
controlPlane: {}
integrations:
  fake:
    provider: fake
    enabled: true
    evidenceFile: provider/evidence.json
surfaces:
  api: { enabled: false }
  web: { enabled: false }
