import type { AgentRunPublicationReport } from '../delivery/contracts/intents.js';
export function projectTerminalAgentRunReport(input: { readonly run: { readonly runId: string; readonly startedAt: string; readonly finishedAt?: string; readonly runner?: { readonly name: string; readonly model?: string | undefined; readonly pool?: string | undefined; readonly cli?: string | undefined } | undefined; readonly workspace?: { readonly path: string } | undefined; readonly agent?: { readonly outcome: 'DONE'|'REJECTED'|'BLOCKED'|'FAILED'; readonly displayBody: string; readonly metadata: Readonly<Record<string,string|number|boolean|null>> } | undefined; readonly failure?: { readonly message: string } | undefined }; readonly stage?: string; readonly awaitingApproval?: boolean }): AgentRunPublicationReport | null {
  if (input.run.finishedAt === undefined) return null;
  const agent=input.run.agent;
  return { runId: input.run.runId, ...(input.stage === undefined ? {} : { stage: input.stage }), ...(input.run.runner === undefined ? {} : { runner: input.run.runner.name, ...(input.run.runner.model === undefined ? {} : { model: input.run.runner.model }), ...(input.run.runner.pool === undefined ? {} : { runnerPool: input.run.runner.pool }), ...(input.run.runner.cli === undefined ? {} : { cli: input.run.runner.cli }) }), startedAt: input.run.startedAt, finishedAt: input.run.finishedAt, displayBody: agent?.displayBody || input.run.failure?.message || 'Runner failed without a response.', outcome: agent?.outcome ?? 'FAILED', ...(typeof agent?.metadata.sessionId === 'string' ? { sessionId: agent.metadata.sessionId } : {}), ...(input.run.workspace === undefined ? {} : { workspacePath: input.run.workspace.path }), metadata: agent?.metadata ?? {}, ...(input.awaitingApproval === true ? { awaitingApproval: true } : {}) };
}